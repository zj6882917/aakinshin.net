{% if tags_list.first[0] == null %}
  {% for tag in tags_list %} 
  	<li><a href="{{ site.url }}{{ site.JB.tags_path }}/{{ tag | resolve_url }}">{{ tag }} <span>{{ site.tags[tag].size }}</span></a></li>
  {% endfor %}
{% else %}
  {% for tag in tags_list %} 
  	<li><a href="{{ site.url }}{{ site.JB.tags_path }}/{{ tag[0] | resolve_url }}">{{ tag[0] }} <span>{{ tag[1].size }}</span></a></li>
  {% endfor %}
{% endif %}
{% assign tags_list = nil %}
